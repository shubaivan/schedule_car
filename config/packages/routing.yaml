framework:
    router:
        # Потрібен для посилань, які генеруються поза HTTP-запитом:
        # нагадування по крону та повідомлення бота.
        default_uri: '%env(APP_BASE_URL)%'

when@prod:
    framework:
        router:
            strict_requirements: null
