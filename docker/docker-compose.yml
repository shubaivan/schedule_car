version: '3'

services:
  db-schedule-car:
    # Name of the image and tag the Dockerfile creates (update this as needed)
    # image: timescale/timescaledb:1.6.0-pg12
    image: postgres:${POSTGRES_VERSION:-16}-alpine
    command: postgres -c work_mem=4GB -c max_parallel_workers_per_gather=8 -c max_connections=900
    #    command: ["-c", "config_file=/postgresql_custom.conf"]
    volumes:
      # Раніше том вішався на /data — postgres його не використовує, і база
      # зникала при перестворенні контейнера.
      - 'psql_db_schedule_car:/var/lib/postgresql/data:rw'
    ports:
      - '127.0.0.1:${POSTGRES_PORT:-5433}:5432'
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    networks:
      - schedule-car
    logging:
      options:
        max-size: 20m
        max-file: "3"

volumes:
  psql_db_schedule_car:

networks:
  schedule-car:
